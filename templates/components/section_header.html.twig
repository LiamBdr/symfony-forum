<header class="pb-5 border-b">
    <{{titleTag}} class="mb-3 text-3xl font-extrabold leading-tight text-gray-900 lg:text-4xl">
        {{ title|raw }}
    </{{ titleTag}}>

    <p class="text-gray-500 text-xl font-light">
        {{ description|raw }}
    </p>
</header>